const isVaild = (s) => {
  const parentheses = 0;
  const brackets = 0;
  const braces = 0;

  const test = s.split("");
  console.log(test);
  const checkDouble = (num) => {
    if (num % 2 === 0) return true;
  };
};

console.log(isVaild("{[]}"));
